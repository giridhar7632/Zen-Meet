## Zen Meet Backend API

To install all the dependencies, run the following command:

```sh
yarn install
```

Make sure to add all the environment variables asmentioned in `.env` file.

To start the server, run the following command:

```sh
yarn run dev
```
